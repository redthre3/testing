@model IEnumerable<FoodFIghtAdmin.Models.RejectedRestaurant>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SwipeList)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SwipeList.RestaurantId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RejectedRestaurantId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RejectedRestaurantId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RejectedRestaurantId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
