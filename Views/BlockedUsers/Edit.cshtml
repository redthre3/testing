@model FoodFIghtAdmin.Models.BlockedUser

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>BlockedUser</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BlockUserId" />
            <div class="form-group">
                <label asp-for="BaseUserId" class="control-label"></label>
                <select asp-for="BaseUserId" class="form-control" asp-items="ViewBag.BaseUserId"></select>
                <span asp-validation-for="BaseUserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BlockedUserId" class="control-label"></label>
                <select asp-for="BlockedUserId" class="form-control" asp-items="ViewBag.BlockedUserId"></select>
                <span asp-validation-for="BlockedUserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
