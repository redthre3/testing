@model IEnumerable<FoodFIghtAdmin.Models.MatchedRestaurant>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AcceptedRestaurant)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AcceptedRestaurant.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MatchRestaurantId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MatchRestaurantId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MatchRestaurantId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
