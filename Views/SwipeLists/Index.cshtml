@model IEnumerable<FoodFIghtAdmin.Models.SwipeList>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MatchSession)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Restaurant)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MatchSession.MatchSessionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Restaurant.RestaurantId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SwipeListId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SwipeListId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SwipeListId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
